let valueA = [];
let valueB = [];
let Iguais = [];

function includesArrays(a, b){
    for( let i = 0; i < a.length; i += 1){
        valueA.push(a[i]);
        return a 
    }
    for( let u = 0; u < b.length; u += 1){
        valueB.push(b[u]);
        return b
    }
    if(a[i] === b[u]){
        Iguais.push();
    }
  }

  includesArrays([1, 2, 3], [4, 2, 4]);